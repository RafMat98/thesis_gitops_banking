apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-service
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0" # Ensure this application is synced before any applications that depend on Redis
spec:
  project: default
  source:
    repoURL: "https://github.com/RafMat98/thesis_gitops_banking.git"
    targetRevision: development
    path: infrastructure/redis
  destination:
    server: "https://kubernetes.default.svc"
    namespace: infra-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
