apiVersion: v1
kind: Service
metadata:
  name: ibm-interface-service
  namespace: banking-apps
  labels:
    app: ibm-interface
spec:
  selector:
    app: ibm-interface
  ports:
    - name: ibm-interface-http-metrics
      protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
